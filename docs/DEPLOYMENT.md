# Deployment Guide — Asteron Global Consultancy (Phase 8, hosting comparison added Phase 9)

Platform-neutral. This document describes what any hosting platform needs to provide and what to configure — it does not assume Vercel, Netlify, AWS, or any other specific provider, and no platform-specific config file (e.g. `vercel.json`, `netlify.toml`) exists in this repository. Pick a platform later; nothing here should need to change as a result.

## 1. What this application is

A Next.js 16 application, currently **entirely statically generated** (every route is prerendered at build time — see [docs/ROUTE_INVENTORY.md](ROUTE_INVENTORY.md)), but built as a standard Next.js server application (`next build` / `next start`), not a static export (`output: "export"` is not set in `next.config.ts`). That means:

- It needs a **Node.js runtime** to serve it (`next start`), or a platform with first-class Next.js server support — not a plain static file host, even though today's content happens to be 100% static.
- `src/app/sitemap.ts`, `src/app/robots.ts`, `src/app/icon.tsx`, and `src/app/opengraph-image.tsx` are all generated at build time and served as static output; no request-time computation happens for them either.

## 2. Build and start commands

```
npm ci
npm run build
npm run start
```

`npm run build` runs `next build`, which also runs a full TypeScript check (`next build` fails the build on any type error — do not skip this by using a "build" script that bypasses it). `npm run start` runs `next start`, which serves the already-built output; it is not a dev server and does not rebuild on file changes.

## 3. Environment variables

See `.env.example` for the complete, current list — there is exactly one:

| Variable               | Required                | Purpose                                                                                                                                                                                                                                                                                                                        |
| ---------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `NEXT_PUBLIC_SITE_URL` | **Yes, for production** | Absolute site URL used for `metadataBase`, canonical links, Open Graph URLs, and `src/app/sitemap.ts`/`robots.ts`. Falls back to `http://localhost:3000` if unset (`src/lib/env.ts`) — the site will build and run without it, but every canonical/OG/sitemap URL will be wrong, which is a launch blocker, not a build error. |

No other environment variable is read anywhere in the codebase today (verified via `src/lib/env.ts`, the single, centralized, Zod-validated access point — see "`.env.example` audit" below). No secrets, API keys, or database connection strings exist because there is no external service or database in this track.

## 4. Node.js version

No `engines` field is currently pinned in `package.json`. This project was developed and tested against a current Node.js LTS release (Node 24 in this environment). Recommend pinning `engines.node` to whatever LTS the chosen platform runs once a platform is selected, and matching CI to the same version — an unpinned engine is a minor, easy-to-fix gap, not a blocker.

## 5. Static assets and caching

- `public/images/`, `public/videos/`, and Next.js's own `_next/static/*` output should be served with long-lived, immutable cache headers (Next.js's default build output is already content-hashed, so this is safe) — most platforms with native Next.js support configure this automatically; confirm it if self-hosting behind a custom reverse proxy.
- No CDN-specific configuration exists or is required by the application itself; any CDN in front of it should respect the cache headers Next.js already sets rather than overriding them with a shorter TTL.
- `next/image` optimization (used nowhere yet, since no photography exists — see docs/MEDIA_ATTRIBUTIONS.md) will need either the platform's built-in image optimizer or a configured custom loader once real photography is added.

## 6. Security headers

Configured centrally in `next.config.ts`, applied to every route: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, a restrictive `Permissions-Policy`, and `Strict-Transport-Security` (only takes effect once served over HTTPS — safe to send in the meantime, browsers ignore it over plain HTTP). No platform-specific header configuration is needed; these are already applied at the application layer regardless of host.

**Content-Security-Policy is deliberately not set** (see the doc comment in `next.config.ts` and docs/DECISIONS.md). A meaningful CSP for this app needs per-request nonces generated in Next.js 16's `proxy.ts` (the successor to middleware) plus every inline script/style wired to that nonce, and pages using them opted into dynamic rendering — none of which exists yet. Shipping a static CSP today would either break the app (if strict) or provide no real protection (if permissive enough not to break it). **This is a genuine, tracked launch consideration, not an oversight:** implement the nonce/proxy architecture and a real CSP before launch if CSP is required by policy, or explicitly accept its absence as a launch decision — either way, do not add a placeholder CSP header that merely appears present without functioning.

## 7. Forms — what must exist before any form is connected

All four forms (`/book-consultation`, `/check-eligibility`, `/insurance-quote`, `/contact`) are currently client-side-only demos — no request leaves the browser (see docs/TECHNICAL_ARCHITECTURE.md §5). Before pointing any form's `SubmissionAdapter` at a real service (Track 2), the destination must independently provide or this application must add: server-side validation mirroring each Zod schema, rate limiting, bot protection (honeypot and/or timing heuristic; CAPTCHA only if abuse is actually observed), CSRF protection appropriate to the chosen transport, spam filtering, request-size limits, TLS-only transport, data minimization, a documented retention period, access controls, an audit trail, and redaction of personal fields from logs. None of this is deployment configuration for the _current_ static site — it is a prerequisite for the _next_ track, recorded here so it isn't forgotten when that track begins.

## 8. Cookies and analytics

No analytics, advertising, or consent-management cookies are set by this application today (verified in `src/content/legal.ts`'s Cookie Policy content and by inspection — no analytics script exists anywhere in the codebase). **No cookie consent banner is needed at launch.** If a future phase adds analytics, the Cookie Policy and this section must be updated together, and a consent mechanism added before any non-essential cookie is set.

## 9. Custom domain and DNS

Standard for any static/SSR Next.js host: point the domain at the platform per its own instructions, provision a TLS certificate (most platforms automate this), and set `NEXT_PUBLIC_SITE_URL` to the final `https://` domain before the production build that will actually be deployed — canonical URLs and the sitemap are baked in at build time, not computed per-request.

## 10. Redirects and legacy URLs

None exist or are needed — this is a new site with no prior URL structure to preserve.

## 11. Monitoring, logging, and error tracking

Not implemented in this track (no error-tracking SDK, no structured logging, no uptime monitoring) — deliberately deferred per docs/DECISIONS.md (C-010, "deferred structured-logging scaffold"). The custom `error.tsx`/`global-error.tsx` boundaries and `not-found.tsx` page handle client-visible error states; nothing reports these anywhere today. Add basic uptime monitoring and, if desired, a client error-reporting service, as a pre-launch or post-launch decision — neither blocks a launch on its own, but launching with zero visibility into runtime errors is worth an explicit decision rather than a silent gap.

## 12. Rollback strategy

Standard for any static/SSR deployment: keep the previous build's deployment artifact available (most platforms do this automatically via deployment history) and roll back to it if a new deployment introduces a regression. No database migrations exist in this track, so a rollback is always safe and reversible — there is no persisted state to reconcile.

## 13. Health checks

No dedicated health-check endpoint exists. Any platform-level health check can simply request `/` (or `/robots.txt`, which is lighter) and expect a 200.

## 14. Backups

Not applicable in this track — there is no database and no persisted user data. The only durable state is the Git repository itself.

## 15. Post-deploy verification checklist

After any production deployment:

- [ ] `/sitemap.xml` and `/robots.txt` resolve and reference the correct production domain (not `localhost`).
- [ ] `/opengraph-image` and `/icon` render correctly (share a link to the homepage on a platform that shows link previews).
- [ ] All four forms show the not-connected message on a valid submission, and no network request is visible in the browser's dev tools network tab.
- [ ] Security headers are present (`curl -I https://<domain>/` and check for `X-Frame-Options`, `Strict-Transport-Security`, etc.).
- [ ] HTTPS is enforced (HTTP requests redirect to HTTPS).
- [ ] Every item under "Blockers" in [docs/PRODUCTION_CONTENT_CHECKLIST.md](PRODUCTION_CONTENT_CHECKLIST.md) has been resolved — this is a content/business decision, not a technical deployment step, and remains the responsibility of the lead architect to confirm before announcing the site as live.

## 16. Dependency and build-integrity checks (run before every production build)

```
npm run format:check
npm run lint
npm run typecheck
npm test
npm run test:e2e
npm run validate:content
npm audit
```

See docs/DECISIONS.md's Phase 8 changelog entry for the dependency-audit classification (outdated packages, none currently a security issue) as of this phase.

## 17. Hosting platform comparison (Phase 9)

No platform has been chosen or configured — no platform-specific config file (`vercel.json`, `netlify.toml`, `wrangler.toml`, etc.) exists in this repository, and none should be added until the lead architect picks one. This section documents what each option would require, based on what this specific application actually does today: 53 fully static/SSG routes (0 dynamic/server-rendered routes as of this review — confirmed via `next build` route output), `next/image` unused (no photography exists yet), `next/font` for self-hosted fonts, `next/og` (`ImageResponse`) for the favicon and default OG image, and a security-headers config in `next.config.ts`.

| Platform                                                                                                                                     | Compatibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Required configuration                                                                                                                                                                                                                                                                                                | Image handling                                                                                                                       | Environment variables                                                                                                                                 | Preview deployments                                                                                                                                                             | Main tradeoff                                                                                                                                                                                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Vercel**                                                                                                                                   | Full — built by the maintainers of Next.js; every feature used here (App Router, `next/og`, `sitemap.ts`/`robots.ts` conventions, security headers via `next.config.ts`) works with zero extra config.                                                                                                                                                                                                                                                                                                                              | None beyond connecting the repo and setting `NEXT_PUBLIC_SITE_URL`.                                                                                                                                                                                                                                                   | `next/image` optimization works out of the box on Vercel's infrastructure — relevant once real photography is added; not used today. | Set in the Vercel dashboard per environment (Production/Preview/Development).                                                                         | Automatic, one per branch/PR, with a unique preview URL — closest match to the "preview deployment" step in docs/RELEASE_CHECKLIST.md.                                          | Best day-one compatibility and least configuration risk; a commercial platform with its own pricing/vendor considerations the lead architect should evaluate independently.                                                                                                                                          |
| **Cloudflare** (Pages / Workers)                                                                                                             | Good, via Cloudflare's Next.js adapter (`@cloudflare/next-on-pages` or the newer OpenNext-based Workers build) — App Router and `next/og` are supported by the current adapter generation, but this is a third-party compatibility layer, not first-party Next.js, so a version bump occasionally lags.                                                                                                                                                                                                                             | Requires the Cloudflare adapter build step in place of plain `next build`; would need its own build command configured in the platform (not added to this repo yet).                                                                                                                                                  | `next/image` needs a custom loader or Cloudflare Images — not automatic like Vercel.                                                 | Set in the Cloudflare dashboard (Pages) or as Worker secrets/vars.                                                                                    | Supported (Pages preview deployments per branch).                                                                                                                               | Strong edge performance and pricing, but the adapter is an extra moving part to keep in sync with Next.js releases — worth a compatibility check against the exact Next.js 16.3.5 version in use before committing to it.                                                                                            |
| **Netlify**                                                                                                                                  | Good, via the official Next.js Runtime (`@netlify/plugin-nextjs`) — supports the App Router and standard `next build` output.                                                                                                                                                                                                                                                                                                                                                                                                       | Requires the Netlify Next.js plugin enabled (not added to this repo yet); otherwise standard `npm run build`.                                                                                                                                                                                                         | Netlify's own image CDN handles `next/image` when the plugin is active.                                                              | Set in the Netlify dashboard (site environment variables, with per-deploy-context overrides).                                                         | Automatic deploy previews per PR.                                                                                                                                               | Comparable to Vercel for this app's needs; slightly less Next.js-native than Vercel since Netlify relies on a maintained-but-third-party adapter, similar tradeoff profile to Cloudflare.                                                                                                                            |
| **Conventional Node.js hosting** (a VM, a container platform, or any host that runs `npm run build && npm run start` behind a reverse proxy) | Full — this is exactly what the app is built for (`next start` is a real Node server), no adapter needed.                                                                                                                                                                                                                                                                                                                                                                                                                           | Reverse proxy (nginx/Caddy) for TLS termination and the security headers already set in `next.config.ts` (no change needed there); a process manager (e.g. `pm2` or a systemd unit) to keep `next start` running and restart on crash; `NEXT_PUBLIC_SITE_URL` set in the server's environment before `npm run build`. | `next/image`'s built-in Node-based optimizer works without any third-party service.                                                  | Set via the OS/container environment or a secrets manager — this project's own `.env.example` pattern (`NEXT_PUBLIC_SITE_URL` only) applies directly. | Not automatic — would need to be built manually (e.g., a separate staging server/subdomain, or a container-per-branch CI job) since there's no platform-native preview feature. | Full control and no vendor lock-in, at the cost of owning uptime, TLS renewal, scaling, and deployment automation yourself — the most work, appropriate if the business already has Node hosting/ops in place.                                                                                                       |
| **Pure static hosting** (S3/CloudFront, GitHub Pages, a plain CDN)                                                                           | **Not compatible as the app is built today.** `next.config.ts` does not set `output: "export"`, and this app uses `next/og` (`ImageResponse`, a server-side route handler under the hood) for the favicon/OG image — a feature `output: "export"` does not support. Static export would require removing or reworking `src/app/icon.tsx`/`opengraph-image.tsx` (e.g., replacing them with static PNG files) and re-verifying every other route has no server-only behavior, which is real, scoped work, not a configuration toggle. | N/A until the above rework happens.                                                                                                                                                                                                                                                                                   | Would need pre-generated static image files (no `next/image` optimization server, no `next/og` at request time).                     | Baked in entirely at build time (no runtime environment variable support at all for a pure static host).                                              | Whatever the CDN/CI platform provides (e.g., GitHub Pages has none natively; most static hosts pair with a separate CI provider for this).                                      | Cheapest possible hosting, but only reachable by giving up `next/og`'s code-generated OG image/favicon (a deliberate Phase 8 choice — see docs/DECISIONS.md) and losing any future flexibility to add server-side behavior without re-platforming. Not recommended unless static-hosting cost is a hard requirement. |

**Recommendation: Vercel**, on the basis of this app's actual architecture — a stock Next.js App Router application using only first-party Next.js conventions (`next/og`, `next/font`, `sitemap.ts`/`robots.ts`, `next.config.ts` headers) with zero custom server code and zero adapter dependency. It requires the least configuration, carries the least platform-compatibility risk, and its automatic preview deployments directly satisfy the "preview deployment" step in docs/RELEASE_CHECKLIST.md §8. Cloudflare or Netlify are reasonable alternatives if there is an existing organizational relationship with either (both fully support this app via their respective Next.js adapters); conventional Node hosting is the right call only if the business already operates its own Node infrastructure and wants to avoid a managed platform; pure static hosting is not recommended without first reworking the two `next/og`-based routes. This is a recommendation for the lead architect to approve — no platform has been selected, configured, or deployed to as part of this review.
