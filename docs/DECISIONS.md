# Architecture Decision Log — Janan Consultancy

This log tracks confirmed decisions, proposed decisions awaiting sign-off, and unresolved questions that require the lead architect's input. Entries should be updated in place (status changed) rather than duplicated when a decision resolves. Dates use ISO format (YYYY-MM-DD).

## How to Use This Log

- **Confirmed:** Decided and binding on implementation.
- **Proposed:** A recommendation has been made (usually in another doc) but not yet explicitly approved by the lead architect.
- **Unresolved:** A genuine business or technical choice with no default assumed; implementation should not proceed on an assumed answer without flagging it here first.

---

## Confirmed Decisions

| ID    | Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C-001 | 2026-09-12 | ~~Project scope for Version 1 is content + lead-generation + admin CMS/lead-management only~~ — **superseded by C-011.** Original text preserved for history; see C-011 for the current scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| C-002 | 2026-09-12 | ~~Technical stack direction: ... PostgreSQL, Prisma, Auth.js ...~~ — **superseded by C-011/C-012.** The current static-website track uses none of PostgreSQL, Prisma, or Auth.js; they are target-track (Strapi/Track 3) considerations only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| C-003 | 2026-09-12 | Temporary brand identity (name, tagline, colour palette) adopted as working defaults for design/documentation purposes, per project brief.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| C-004 | 2026-09-12 | Insurance is modeled as a distinct entity (`InsuranceProduct`) separate from `Service`, with mandatory disclosure/disclaimer/eligibility/exclusions fields, per the insurance requirements in the brief.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| C-005 | 2026-09-12 | English-only for Version 1; no i18n infrastructure built.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C-006 | 2026-09-12 | No online payments, policy issuance, application submission, automated decisioning, student portal, or native mobile app in Version 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| C-007 | 2026-09-12 | Phase 1 stack versions locked at scaffolding time (current stable, mutually compatible, no beta/RC): Next.js 16.3.5, React 19.2.8, TypeScript 5, Tailwind CSS 4, shadcn/ui 4.21.0, Zod 4.6.2, React Hook Form 7.88.0, Vitest 5.0.0, Playwright 1.63.0, ESLint 9 (eslint-config-next pins this; ESLint 10 exists but isn't yet supported by eslint-config-next), Prettier 3.9.6. Full list in README.md "Technical Stack".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C-008 | 2026-09-12 | shadcn/ui initialized with the **Radix** primitive library (`-b radix`), not the CLI's current Base UI default, with a hand-written `cn()` in `src/lib/utils.ts` built on `clsx` + `tailwind-merge` (not the shadcn-maintained `cn` package the tool defaults to). Reason: the phase brief explicitly required `clsx` and `tailwind-merge` as first-class dependencies; the CLI's newest default swaps both out.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| C-009 | 2026-09-12 | Content-Security-Policy is deferred, not implemented, in Phase 1. Other security headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy, Strict-Transport-Security) are set in `next.config.ts`. Reason: a non-misleading CSP needs per-request nonces via a `proxy.ts` (Next 16's renamed middleware) and dynamic rendering on nonce-consuming pages — that architecture doesn't exist yet, and the phase brief explicitly said not to ship a misleading static policy. Revisit alongside the auth/proxy layer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| C-010 | 2026-09-12 | No `loading.tsx` added in Phase 1 — there are no async data boundaries yet to show a fallback for. Add per-route once real data fetching exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| C-011 | 2026-09-12 | **Major scope correction, issued mid-Phase-1 by the lead architect:** the initial release is a **static marketing website** with no database, no ORM, no authentication, no admin dashboard, no API server, and no persistent storage of any kind. Strapi CMS may be integrated in a later phase (Track 3) but is not part of the current implementation. The roadmap is now four tracks: (1) Static website, (2) Form-service integration, (3) Strapi CMS integration, (4) Advanced portal features (speculative). Every project document was updated to reflect this. See docs/IMPLEMENTATION_PLAN.md for the full track breakdown.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| C-012 | 2026-09-12 | Following C-011: the `/admin` placeholder route and page (added earlier in Phase 1) were deleted before the phase completed. No PostgreSQL, Prisma, or Auth.js package was ever installed, so no dependency removal was needed. A content-access-layer foundation was added instead (`src/types/content.ts`, `src/content/`, `src/lib/content/`) so pages read local typed content through swappable `async` functions designed to later call a Strapi API without a UI rewrite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| C-013 | 2026-09-12 | Forms in the current track are client-side-only with a demo-mode submission adapter that must never claim a submission was saved or delivered. Real delivery is Track 2 work; persistent lead storage/status/assignment is Track 3 work (see docs/PRODUCT_REQUIREMENTS.md §3.2 and docs/TECHNICAL_ARCHITECTURE.md §5, §15).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| C-014 | 2026-09-13 | Every planned route in docs/SITEMAP.md §1 was scaffolded in Phase 2 as a one-line page that renders a shared `ComingSoon` component (breadcrumb + title + generic copy + consultation CTA), rather than leaving unfinished nav links pointing at nothing. Reason: real header/footer navigation needs real, working hrefs; a shared component avoids duplicating placeholder markup across ~20 route files. Phase 3 replaces each stub with real content one route at a time.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| C-015 | 2026-09-13 | Desktop navigation switches to the mobile Sheet-based menu at the `xl` breakpoint (1280px), not the more common `lg` (1024px). Reason: with 9 top-level nav items + CTA + logo, `lg` produced 185px of horizontal overflow at exactly 1024px width (manually verified, then fixed and re-verified with a Playwright/Node screenshot check).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| C-016 | 2026-09-13 | Footer social links (Facebook/Instagram/LinkedIn/YouTube) render as neutral two-letter initial badges, not brand icons. Reason: the installed `lucide-react` version ships no brand/social glyphs at all (they were removed from the package); initials avoid both a new icon-library dependency and any risk of approximating a trademarked logo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| C-017 | 2026-09-13 | `docs/SITEMAP.md` gained `/resources/faqs`, not present in the original static route list. Reason: the Phase 2 brief's Resources dropdown explicitly requires an "FAQs" entry, and no existing planned route covered it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| C-018 | 2026-09-13 | Phase 3 ("Complete static homepage") is scoped to the homepage only. Every other route stays on Phase 2's `ComingSoon` placeholder. The original docs/IMPLEMENTATION_PLAN.md Phase 3 description (build every entity's content + every detail page) is narrower than what was actually asked for this session and is superseded by what was delivered; the remaining detail-page work is a new, not-yet-scheduled continuation rather than silently folded into Phase 4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| C-019 | 2026-09-13 | The homepage uses **no photographs**. This environment has outbound network access to image CDNs but no image-search API, so there is no reliable way to find, verify the licence of, and correctly attribute a _specific_ relevant stock photo without guessing at CDN URLs from memory — exactly the "unstable random-image URL" risk the brief asked to avoid. Per the brief's own fallback instruction, the hero and every card use original CSS/SVG compositions (extending the `SiteLogo` motif) and Lucide icons instead. See docs/MEDIA_ATTRIBUTIONS.md for the full rationale and the replacement procedure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| C-020 | 2026-09-13 | `src/components/ui/accordion.tsx` (needed for the FAQ section) was hand-written directly against `radix-ui`'s bundled `Accordion` export rather than fetched via the shadcn CLI — `ui.shadcn.com`'s registry JSON endpoints timed out repeatedly during this session (the bare domain responded; only `/r/...` registry requests failed). It follows the exact conventions of the other hand-maintained files in `src/components/ui/`. `tw-animate-css` (already a dependency) already ships the `animate-accordion-down`/`-up` keyframes it needs, so no new dependency was added. Retry the CLI in a future session if registry access is needed again.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C-021 | 2026-09-13 | A stray `cn` npm package (the shadcn CLI's own default class-merging helper, distinct from and unrelated to this project's `cn()` utility in `src/lib/utils.ts`) was added to `package.json` as a side-effect of an earlier failed `shadcn add accordion` attempt that got partway through "Installing dependencies" before the registry timeout. Removed via `npm uninstall cn` before completing this phase — confirmed nothing in `src/` ever imported from it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| C-022 | 2026-09-13 | `SectionHeading` gained an optional `headingId` prop so every homepage section's `aria-labelledby` reference resolves to a real element id — the initial implementation set `aria-labelledby="x-heading"` on each `<section>` without ever rendering a matching `id`, which is an invalid/dangling ARIA reference. Caught during a self-review before this phase's accessibility checks, not by an external report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| C-023 | 2026-09-13 | The Insurance section is the one homepage section with an inverted (navy) background, and it does **not** use the shared `SectionHeading` component — that component hardcodes light-mode-oriented text tokens that would fail contrast on a dark surface. It hand-rolls equivalent markup instead. This is a narrow, documented exception, not a change to `SectionHeading` itself (every other section still uses it correctly).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| C-024 | 2026-09-13 | The Phase 2/3 lightweight `DestinationSummary` type was removed and unified into a single, full `Destination` type (26+ fields — see docs/CONTENT_MODEL.md §3) used everywhere a destination is rendered: header/footer navigation, the homepage destinations section, and the new `/study-abroad` hub/destination pages. Reason: keeping a separate summary type would have meant either duplicating destination content across two records (drift risk) or having the homepage read a subset of the same full record anyway — a single type is simpler and matches CLAUDE.md's anti-speculative-abstraction guidance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| C-025 | 2026-09-13 | **Content Accuracy policy** (applies to all study-abroad, destination, and visa/immigration-adjacent content): no precise visa fees, no guaranteed processing times, no fixed financial thresholds, no promised/guaranteed work rights or post-study work eligibility, no universal deadlines, no invented scholarships or university partnerships, no detailed legal/immigration advice. All such content uses "general guidance"/"requirements may change" language and directs readers to the relevant official government or institution source. Every `Destination` record carries a `lastReviewed` ISO date. No external sites were browsed to author this content; `lastReviewed` records when the content was last checked against this policy, not that live official sources were re-verified on that date — see the file-level comment in `src/content/destinations.ts`. Enforced by `src/content/destinations.test.ts` and `src/content/study-abroad.test.ts` (banned-phrase scans, no currency-figure regex matches, "official/government" required in every visa-guidance field). |
| C-026 | 2026-09-13 | **Check-Eligibility Form Deferred:** `/check-eligibility` ships as an informative-only static page (general eligibility factors + FAQ), with no interactive form. Reason: no form foundation exists anywhere in this codebase yet — React Hook Form and Zod are installed dependencies but zero form components have been built in any prior phase — and a bespoke demo form here would duplicate the Track 2 form-service integration work rather than reuse it. This follows the phase brief's own explicit fallback instruction for this exact situation. Revisit once a shared form foundation exists (Track 2, or an earlier phase that establishes one).                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| C-027 | 2026-09-13 | Destinations get **no photography**, extending C-019's homepage precedent to Phase 4 for the same reason (no reliable way to find/verify/attribute a specific relevant licensed photo in this environment). Each destination instead gets a distinct Lucide icon (`visualIcon` field) rendered on the existing gradient-panel treatment, verified to exist in the installed `lucide-react` version before use. This also avoids the "flag-grid" look the brief explicitly warned against — `flagLabel` is a small text badge alongside the icon, never the sole or primary identifier.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| C-028 | 2026-09-13 | The generic 10-step study-abroad application process and the undergraduate/postgraduate study-level pages are each authored **once** and rendered everywhere they're needed, rather than duplicated per destination or per study level: `genericApplicationProcess` (`src/content/study-abroad.ts`) is rendered on the hub and every destination page (with only a short destination-specific note alongside it, `Destination.applicationProcess`); `StudyLevelTemplate` (`src/components/study-abroad/StudyLevelTemplate.tsx`) is the single component rendering both `/study-abroad/undergraduate` and `/study-abroad/postgraduate` from separate `StudyLevelPageContent` records. Per the phase brief's explicit "do not build separate duplicated page implementations" instruction.                                                                                                                                                                                                                                                                                                        |
| C-029 | 2026-09-13 | Resolving a content-driven icon name (`resolveIcon(name)`) to a JSX tag directly in a component's top-level render body (i.e., outside a `.map()`/list-rendering callback) trips the `react-hooks/static-components` ESLint rule ("component created during render"), even though `resolveIcon` returns a referentially-stable lookup-table value. Existing icon-resolving code was always inside a `.map()` callback, which the rule doesn't flag. Fixed in the one new case that needed a single (non-list) icon (`DestinationHero.tsx`) by rendering via `createElement(resolveIcon(name), props)` instead of assigning to a capitalized JSX-tag variable. Future single-icon-in-render cases should follow the same pattern.                                                                                                                                                                                                                                                                                                                                                                |

| C-030 | 2026-09-13 | The Phase 2–4 lightweight `ServiceSummary` type was removed and unified into a single, full `Service` type (see docs/CONTENT_MODEL.md §8), the same unify-rather-than-duplicate move made for `Destination` in Phase 4 (C-024) — for the same reason (one content shape, no drift risk between a summary and a full record). Two content changes went with it: the `university-selection` service was renamed to `university-course-selection` (slug and title) to match the phase brief's naming and route list — confirmed via grep that no other content referenced the old slug before renaming; and a new `scholarship-guidance` service was added (`isFeatured: false`, so the homepage's existing 6-service teaser and its test are unaffected) to satisfy the brief's explicit `/services/scholarship-guidance` route requirement, which supersedes the Phase 2/3-era comment in `src/content/services.ts` stating scholarship guidance would **not** get a Service record. |
| C-031 | 2026-09-13 | Insurance content is implemented locally as `InsuranceService` (docs/CONTENT_MODEL.md §9) — the local name differs from the target Strapi entity name `InsuranceProduct` only because `InsuranceService` reads more naturally alongside `Service` in the same file; the shapes map field-for-field. The phase brief's required global insurance disclosure text is a separate, page-agnostic constant (`insuranceGlobalDisclosure`) rendered identically on the hub, all three detail pages, and the quote page — kept distinct from the shorter, already-tested `insuranceDisclaimer` homepage teaser string from Phase 3 (docs/CONTENT_MODEL.md §0) rather than replacing it, so the existing Phase 3 homepage test needed no change. |
| C-032 | 2026-09-13 | **Quote-Form Demo Behavior:** `/insurance-quote` is the first real form built in this codebase (Track 1 has had none since Phase 1 scaffolding — see C-026). It establishes the general-purpose submission-adapter pattern the brief anticipated: `submitDemoForm()` (`src/lib/forms/submissionAdapter.ts`) is a plain async function returning a discriminated `SubmissionResult` (`"not-configured" \| "success" \| "error"`) with no network call, no console logging, no `localStorage`/`sessionStorage` write, and no URL mutation — verified by dedicated unit tests spying on `fetch`/`console.log`. On a valid submission the form shows the required verbatim message ("Online quote submission is not connected yet...") with real `tel:`/`mailto:`/`wa.me` links from `getSiteContent()`, never a fake success state. This adapter is intentionally generic (not insurance-specific) so Track 2 replaces its body once, and so a future Consultation/Contact/Eligibility form (Phase 6+) can reuse it immediately — directly unblocking the "revisit `/check-eligibility`" note in C-026. |
| C-033 | 2026-09-13 | Radix `Select`/`Checkbox` (via `react-hook-form`'s `Controller`) needed two jsdom polyfills that this project's test suite had never required before Phase 5's form: `ResizeObserver` and pointer-capture APIs (`hasPointerCapture`/`setPointerCapture`/`releasePointerCapture`) plus `scrollIntoView`. Added as minimal global stubs in `vitest.setup.ts` (guarded by `typeof ... === "undefined"` so they never shadow a real implementation) rather than per-test-file mocks, since any future Radix-based form will hit the same gap. |
| C-034 | 2026-09-13 | `insuranceQuoteSchema`'s `numberOfTravellers` field uses `z.coerce.number()` (native `<input type="number">` values arrive as strings). This makes Zod's _input_ type `unknown` and its _output_ type `number` for that field — assigning `useForm<T>` a single type `T` (`z.infer`, i.e. the output shape) fails type-checking against `zodResolver`'s expected input shape. Fixed by exporting both `z.input<>` (`InsuranceQuoteFormInput`, what react-hook-form actually manages) and `z.output<>` (`InsuranceQuoteFormValues`, what `onValid` receives after coercion) from `src/lib/validation/insuranceQuote.ts`, and using react-hook-form's 3-generic `useForm<Input, Context, Output>` form. Apply the same split to any future Zod schema that uses `z.coerce`. |
| C-035 | 2026-09-13 | `SampleContentBadge` (Phase 3) was never exercised with a long label before Phase 5's `/insurance-quote` demo-form notice ("Development demo form — not yet connected to a submission service") — its underlying `Badge` primitive is `whitespace-nowrap`/`w-fit` by design (correct for short labels like "Demo content"), which overflowed the viewport at 320px width. Fixed at the call site only (`className="h-auto w-full max-w-full text-wrap whitespace-normal py-1 sm:w-fit"` on that one usage) rather than changing the shared `Badge`/`SampleContentBadge` default, since every other usage's short labels are unaffected and correctly want the compact, single-line treatment. |

| C-036 | 2026-09-13 | **Legal route naming:** the Phase 6 brief referred to the legal pages informally as `/privacy`, `/terms`, `/cookies`, `/disclaimer`. Rather than adding a second, parallel flat URL set alongside the already-scaffolded `/legal/privacy-policy`, `/legal/terms-of-service`, `/legal/cookie-policy`, `/legal/disclaimer` (built in Phase 2, already wired into `footerLinkGroups`/`legalLinks` and documented in docs/SITEMAP.md as a deliberate, lead-architect-confirmed "flatter route list" correction), real content was implemented at the existing `/legal/*` paths. No duplicate or orphaned legal route exists. Flag for confirmation if the lead architect specifically wants the flat paths instead. |
| C-037 | 2026-09-13 | **FAQ route consolidation:** Phase 2 had scaffolded `/resources/faqs` as a `ComingSoon` placeholder for what the Resources dropdown called "FAQs". Phase 6's brief asks for a standalone `/faq` page; since both were the same stub with no real content ever built at the old path, `/resources/faqs/page.tsx` was deleted (confirmed via `git status`/file contents that it was still a pure placeholder) and the Resources dropdown's "FAQs" entry now points at `/faq`. No content or route was lost — nothing existed there beyond the shared stub. |
| C-038 | 2026-09-13 | **Team Placeholders:** `/team` and the About-page team preview render exclusively role-based cards (`TeamMember.isPlaceholder: true`, `placeholderNote: "Profile to be added."`) — no real or fictional staff name, photo, biography, or credential exists anywhere in this codebase. `SampleContentBadge` (reused, not a new component) renders the placeholder note in the same visual treatment as "Demo content" elsewhere, keeping one consistent "this isn't real yet" pattern site-wide. See "How to replace team placeholders" in README.md. |
| C-039 | 2026-09-13 | **Success Stories Policy:** `/success-stories` combines both options the brief offered rather than choosing one — a polished empty-state paragraph explaining verified stories require student permission, plus three "Demo content"-labelled example-journey cards demonstrating the intended `SuccessStory` structure (destination/study-level/subject/summary/quote only — no offer letters, passport numbers, or other private records, and no `Review`/`AggregateRating` structured data). This gives the page real content to show today without ever implying a fabricated testimonial is genuine. |
| C-040 | 2026-09-13 | **Events Policy:** `/events` ships exactly one sample entry (`isSampleContent: true`, `eventStatus: "schedule-tbd"`), with `startAt`/`endAt`/`speaker`/`location`/`capacity`/`registrationUrl` all left `undefined` rather than populated with placeholder-but-plausible values — the type itself makes these fields optional so "not yet known" is representable without inventing data. `/events/[slug]` never emits `Event` JSON-LD for a sample or schedule-tbd entry (checked via `event.isSampleContent`/`event.eventStatus` before emitting), per the brief's explicit prohibition on Event structured data for non-genuine events. |
| C-041 | 2026-09-13 | **Scholarship Directory Policy:** `/scholarships` publishes zero active scholarships. Two records exist, both named `"Content Template — ..."` and typed `isTemplate: true`, to demonstrate the intended card/detail structure per the brief's explicit optional suggestion. `openingDate`/`deadline`/`officialUrl` are left `undefined` on every record (the detail page renders "Opening date and deadline: to be confirmed once verified" instead of blank), since the brief prohibits publishing an unverified deadline. |
| C-042 | 2026-09-13 | A second `SampleContentBadge` long-label overflow (see C-035) appeared on `/scholarships` — `ScholarshipCard`'s "Content template — not an active scholarship" label overflowed 320px width by 4px for the same `whitespace-nowrap` reason. Fixed with the same wrapping-class pattern as C-035, applied at this second call site. Any future long `SampleContentBadge` label should apply this pattern proactively rather than waiting for a horizontal-overflow test to catch it. |
| C-043 | 2026-09-13 | **Form Architecture (Phase 7):** the Phase 5/6 pattern of each form owning its own local `Field` wrapper, `describedBy()` helper, submitted-state, and ad hoc `submitDemoForm()` import was consolidated into one shared architecture before adding two more forms: `SubmissionAdapter`/`SubmissionResult` (`src/types/forms.ts`) replaces the old adapter's interface; `notConfiguredAdapter` (`src/lib/forms/adapters/notConfiguredAdapter.ts`) is the sole Track 1 implementation and is the only thing every form calls on a valid submission; `src/lib/forms/messages.ts` centralizes every verbatim string (demo notice, not-connected message, consent label, sensitive-data warning, preference-only note, error-summary heading) so the required wording cannot drift between forms; `FormField`/`FormErrorSummary`/`FormDemoNotice`/`FormSubmittedNotice`/`ConsentField` (`src/components/forms/`) and the `useDemoFormSubmit` hook replace each form's local copies. This is deliberately a small set of composable pieces, not a schema-driven form-builder or generic form framework — the brief explicitly asked for the former. `InsuranceQuoteForm` and `ContactForm` were refactored onto it alongside the two new forms so all four are consistent; the old `src/lib/forms/submissionAdapter.ts` (C-032) was deleted rather than kept alongside the new one. |
| C-044 | 2026-09-13 | **Duplicate demo-notice removal:** once `FormDemoNotice` existed, `/insurance-quote` and `/contact` each still carried their own Phase 5/6 page-level "demo form" badge/notice with slightly different wording from the new shared copy — rendering both would have shown the same notice twice with inconsistent text. The page-level notices were removed (and, on `/contact`, the now-unused `contactFormPrivacyNotice` content/accessor removed with it) in favour of the one `FormDemoNotice` each form now renders itself; the corresponding page tests were updated to assert the new shared wording instead of the old bespoke wording. `/contact`'s `temporaryValuesNote` (about placeholder phone/email/address, not the form) was kept — it is not part of the form-demo notice and remains accurate. |
| C-045 | 2026-09-13 | **Eligibility form — conditional fields:** English-test (`englishTestType`/`englishTestScore`) and study-gap (`studyGapDetails`) fields are hidden in the UI until their toggle checkbox (`englishTestTaken`/`hasStudyGap`) is checked, and are validated conditionally via a single `.superRefine()` on `eligibilitySchema` rather than making them unconditionally required or unconditionally optional. A `cleanEligibilityPayload()` helper clears the irrelevant hidden value before a valid payload reaches the submission adapter, so a value typed into a field and then hidden again (by unchecking the toggle) can never leak into any future processing — defence in depth, since the current adapter discards the payload entirely regardless. |
| C-046 | 2026-09-13 | **Eligibility form — single page, not multi-step:** the Phase 7 brief permitted either a multi-step wizard (with in-memory-only state) or a single grouped page "if that is more robust." A single page grouped into five labelled sections (Personal / Academic / English language / Study preferences / Additional) was chosen: it avoids building step-state management, back/continue navigation, and step-announcement logic for a form whose complexity comes from conditional fields, not from an inherently sequential flow — and it guarantees no partially-entered data can be lost by navigating between steps, since there are none. Preferred destinations use native checkboxes in a `<fieldset>` (explicitly permitted by the brief) rather than a custom multi-select widget. |
| C-047 | 2026-09-13 | **Header breakpoint moved from `xl` to `2xl` (Phase 8):** the Phase 8 responsive QA matrix (`e2e/responsive.spec.ts`, 6 viewport widths × 13 pages) caught a real, previously-undetected 29px horizontal-overflow bug at exactly 1280px width — the `xl` breakpoint chosen in C-015 to fix an earlier, larger overflow at `lg` (1024px). At exactly `xl`, the 9-item desktop nav, logo, and header CTA button together still didn't fit within the 1152px (`max-w-6xl`) container. Rather than shrinking nav item padding/text (which would reduce click-target size and touch usability), the show/hide breakpoint for `HeaderDesktopNav`/`HeaderMobileMenu` was moved one step further out, to `2xl` (1536px) — verified overflow-free at 1280, 1440, 1536, and 1600px. This is the same fix pattern as C-015, applied once more; if a future nav-item addition reintroduces overflow even at `2xl`, the next step should be reducing item spacing/font-size at that breakpoint rather than moving the breakpoint out again indefinitely. |
| C-048 | 2026-09-14 | **Container variant system (Phase 10 visual redesign):** the single `max-w-6xl` (~1152px) `Container` used for every section — including the header and footer — was identified by the lead architect as the root cause of "header/footer content width too narrow": at 1440px+ viewports the header/footer shell filled the full width but their inner content stayed capped at 1152px, leaving large empty navy gutters either side. `Container` gained a `variant` prop (`content` — the original 1152px, now the explicit default so all ~64 existing call sites needed no change; `wide` — ~1600px, used for the header, footer, announcement bar, and large marketing bands; `reading` — ~720px, reserved for future long-form prose; `fullBleed` — no max width) rather than introducing a second, parallel container component. Responsive padding was widened at the same time (`lg:px-8` → `lg:px-10`, added `2xl:px-16`) per the brief's explicit 16/24/40/64px mobile→wide-desktop spec. |
| C-049 | 2026-09-14 | **Hero redesign — asymmetric composition kept inside the `wide` container, not viewport-bled (Phase 10):** the brief's hero concept describes photography/visual elements that could "bleed" to the viewport edge. True edge-to-edge bleed (negative margins keyed to `100vw` minus the container width) was deliberately not used — it is exactly the class of layout technique responsible for the C-015/C-047 header overflow bugs already found and fixed twice in this project, and a viewport-relative negative margin is fragile precisely at the width extremes (very narrow or very wide) the redesign is required to support. Instead, the hero uses an unequal two-column split inside `Container variant="wide"` (~45%/55%, `lg:grid-cols-[minmax(0,1fr)_minmax(0,1.2fr)]`) with a large, tall, visually dominant right-hand panel — asymmetric and dramatic in effect without the overflow risk. Verified overflow-free at 320–1920px via `e2e/responsive.spec.ts` and manual screenshot review. |
| C-050 | 2026-09-14 | **No real photography sourced for the Phase 10 redesign.** Network access was available in this session (verified), but Unsplash's keyword-based "Source" endpoint — the only mechanism that would have allowed sourcing topically-relevant, properly-licensed images without a paid API key or manual browsing — returned `503` (confirmed dead/deprecated). Rather than embedding images picked blind (unverifiable relevance, risk of looking "staged or unrelated" per the brief's own prohibition) or using an unrelated generic-photo service, the redesign follows the brief's explicit fallback: "If network access is unavailable, use strong designed SVG/colour compositions and list the images still required." The hero, service cards, and footer now use original layered SVG/gradient compositions (abstract skyline silhouette, route/pin motifs, ambient glow) rather than photography. See docs/MEDIA_ATTRIBUTIONS.md for the specific real-photography opportunities this leaves open. |
| C-051 | 2026-09-14 | **`WhyChooseSection` removed from the homepage, content kept (Phase 10 text-density reduction):** `WhyChooseSection` (4 icon+title+description items, "Why choose Janan") and `TrustStrip` (4 icon+title+description items, directly above it) carried near-duplicate messaging in an identical visual format — a direct instance of the "stack of text and cards" complaint. Rather than rewriting one of them, `WhyChooseSection` was removed from `src/app/(public)/page.tsx` and the now-unreferenced `src/components/home/WhyChooseSection.tsx` component was deleted (dead code — nothing else imported it). `whyChooseReasons`/`getWhyChooseReasons`/`WhyChooseReason` in the content layer were deliberately **not** deleted: the data is real, tested, and policy-compliant (no fabricated claims), and remains available if a future page (e.g. `/about`) wants it — only the redundant presentational component and its homepage placement were cut. |
| C-052 | 2026-09-14 | **Footer link groups made comprehensive, not "deliberately smaller than the header" (Phase 10):** the original footer intentionally listed only 2-7 links per group (docs/SITEMAP.md §4, superseded by this decision) — flagged by the lead architect as "too few links" against an explicit requirement that every destination, every service, and both non-visitor insurance types be reachable from the footer. `footerLinkGroups` (`src/content/navigation.ts`) now derives its Study Abroad and Services groups from `destinations`/`services`/`insuranceServices` the same way `headerNavigation`'s dropdowns already did, so the footer can never drift from the header's own data. Visitor insurance is deliberately excluded from the footer's Services group (not from the header) to match the brief's explicit footer link list, which names only Student Health and Travel insurance. The resulting 12-14-link groups render as a `sm:grid-cols-2` two-column mini-grid (`SiteFooter.tsx`) rather than one tall column, per the brief's "use columns... where appropriate." |
| C-053 | 2026-09-14 | **Visual Language Reset — Phase 10B "MANDATORY RESET" supersedes Phase 10 (C-048–C-052) entirely, same day.** The lead architect rejected the Phase 10 redesign outright (navy/gold palette, header showing two nav items active simultaneously, dashboard-toolbar-style nav buttons, a 12-14-link-per-group footer that reproduced the full sitemap, a dark oversized hero) and issued a complete replacement brief: new palette (ink/blue/teal/coral on white, C-054), a 6-item header nav with curated dropdowns and a single "Book a Consultation" CTA (phone removed from the desktop row), a route-ownership active-nav function (C-055), a rebuilt hero and a 13-section homepage rhythm (C-056), and a rebuilt 5-column footer capped at exactly 6 links per group with placeholder social links hidden entirely rather than shown disabled (C-057). Explicitly scoped to **Phase A only** — inner pages are deliberately unchanged; `docs/VISUAL_REDESIGN_MATRIX.md` was created to plan that follow-on work without starting it. |
| C-054 | 2026-09-14 | **New design-token palette (Phase 10B):** `:root` in `globals.css` replaced the Phase 10 navy/gold tokens with the brief's exact hex values — ink `#14213D`, primary blue `#3157F6`, deep blue `#173B8F`, teal `#12A594`, coral `#FF7A59`, plus pale-blue/pale-mint/warm surface tints and a pure-white `--background` (previously a soft off-white). `--primary`/`--ring`/the `accent` button variant moved from `brand-navy`/`brand-gold` to `brand-ink`/`brand-blue`. Coral was verified by manual WCAG 2.2 contrast calculation to be unsafe as a text/icon color on white (~2.6:1) and is therefore used only as a background tint behind dark ink text/icons (the Scholarship section's motif, the Final CTA band's decorative glow) — never as CTA text or a filled button color, matching the brief's "avoid gold as primary CTA color" instruction generalized to coral. |
| C-055 | 2026-09-14 | **Route-ownership active-navigation, replacing prefix matching (Phase 10B):** the Phase 10 header's "Study Abroad" and "Destinations" nav items both used `pathname.startsWith(href)` against the same underlying `/study-abroad` href, so any `/study-abroad/[destination]` URL activated both simultaneously — the lead architect's most specific named defect. `src/lib/navigation-active.ts` replaces prefix matching with one pure function, `getActiveNavGroupId(pathname)`, that returns exactly one `NavGroupId | null` per the brief's explicit route-ownership table (e.g. `/study-abroad/undergraduate` → Study Abroad, but `/study-abroad/united-kingdom` → Destinations). `HeaderDesktopNav` and `HeaderMobileMenu` both consume it, so desktop and mobile can never disagree about which group is current. 12 unit tests plus a dedicated `e2e/navigation-active.spec.ts` (7 routes, asserts `[aria-current="page"]` count is exactly 0 or 1) cover every route named in the brief. |
| C-056 | 2026-09-14 | **Homepage rebuilt to the brief's 13-section blueprint (Phase 10B), not a palette-only pass.** `FeaturedUniversitiesSection` and `EventSection` were removed from `/` (not part of the new blueprint); a new `StudyAbroadIntroSection` was added. `DestinationsSection` and `ResourcesSection` became image-led galleries (1 featured + smaller tiles) instead of uniform card grids. `SuccessStoriesSection` collapsed from 3 near-identical placeholder cards to one compact panel (one "Demo content" badge instead of three). `InsuranceSection` moved off a dark `bg-primary` card grid onto a light `surface-pale-mint` split layout showing only the top 3 highlights. `FaqSection` capped the homepage accordion at 5 of the 6 available questions with a "View All FAQs" link. The hero (`Hero.tsx`/`HeroVisual.tsx`) was deleted and rebuilt on a 12-column split with the brief's exact copy ("Build your future beyond borders.") and a light pale-blue/mint visual with at most two floating elements, replacing the Phase 10 dark layered-skyline composition. |
| C-057 | 2026-09-14 | **Footer capped at exactly 6 links per group and placeholder socials hidden (Phase 10B), reversing C-052's "comprehensive" footer the same day.** The Phase 10 footer's `.map()`-derived 12-14-link groups (every destination, every service) were flagged as "reproduces the complete sitemap instead of guiding users." `footerLinkGroups` (`src/content/navigation.ts`) now hand-curates exactly 6 links per group (Study / Services / Company) instead of deriving from the full content arrays, and the footer dropped its `sm:grid-cols-2` internal sub-grid for a single column per group (all 6 links fit on one line each at 1440px without wrapping). The brand column's social icons — 4 "Fb/Ig/In/Yt" initials-only circles pointing at `href: "#"` — are now filtered by `social.disabled` and the whole `<ul>` omitted when none are real, rather than rendered as non-functional placeholder buttons. The long multi-paragraph legal disclaimer was removed from the footer (it already exists in full on `/legal/disclaimer`) in favour of one line, "Information only. No outcome is guaranteed." |
| C-058 | 2026-09-14 | **Header/mobile-menu breakpoint moved back from `2xl` to `xl` (Phase 10B), partially reversing C-047 the same day.** C-047 moved the desktop-nav breakpoint out to `2xl` (1536px) to fix an overflow caused by a 9-item nav, a phone number, and a longer CTA label all competing for space at `xl` (1280px). The Phase 10B header removed the phone number entirely, cut the nav to 6 items, and shortened the CTA to "Book a Consultation" — the original overflow cause no longer exists. Left at `2xl`, the header would have shown the mobile hamburger menu (not the real desktop nav/dropdowns) at 1440px, one of this phase's two mandatory screenshot breakpoints — this was caught during the required screenshot-inspection step, not assumed. Moved to `xl` (1280px) and re-verified overflow-free at 1024 (falls back to mobile, a deliberate choice, not a bug), 1280, 1366, 1440, and 1920 via the full `e2e/responsive.spec.ts` matrix. |
| C-059 | 2026-09-14 | **Three defects found via required screenshot inspection, not assumed from code (Phase 10B):** the brief required capturing and _inspecting_ screenshots before reporting, "not as documentation only." Three real defects were caught this way that no automated test covered: (1) `SheetContent`'s own base classes (`data-[side=right]:w-3/4`) were silently winning over the mobile menu's intended `w-full` override via Tailwind's variant-based class-merge order — the panel rendered at ~75% width, not "near-full-screen" as the brief required; fixed by adding a matching `data-[side=right]:w-full` override so `cn()`/`tailwind-merge` resolves it correctly. (2) The fixed mobile `MobileQuickActions` bar's bottom padding was applied to `<main>` only, but `SiteFooter` renders as `<main>`'s sibling, not its child — so the footer's own bottom row (which wraps to two lines on narrow screens) was partly hidden underneath the bar; the padding was moved from the layout's `<main>` onto the footer's own closing legal row (`src/app/(public)/layout.tsx`, `SiteFooter.tsx`). (3) `SectionHeading`'s eyebrow label had a sitewide `uppercase` CSS transform, directly violating the brief's "avoid all-caps everywhere" typography rule even though every eyebrow string in content was already authored in sentence case — the transform was the only thing making them render as caps; removed. |
| C-060 | 2026-09-14 | **`ScholarshipSection` and `FinalCtaSection` redesigned during the same screenshot-review pass (Phase 10B), not left as Phase 10 leftovers.** `ScholarshipSection` still carried the Phase 10 "icon + 4-card grid on `bg-muted/50`" layout after the initial reset pass; rebuilt to the brief's "warm image-led horizontal feature" — a `surface-warm`/coral-tinted original visual on one side, a 3-item checklist (sliced from the shared 4-item `scholarshipHighlights` content, which is left intact for `/scholarships`) and one CTA on the other. `FinalCtaSection` was initially given a `bg-brand-ink` background to be "bold, memorable" per the brief, but review showed it then sat directly above the footer's own ink-colored top CTA row with only a 1px border between them, reading as one undifferentiated dark block; changed to a `brand-blue`→`brand-blue-dark` gradient instead, which stays bold while remaining visually distinct from the ink footer immediately below it. Its buttons were rebuilt with explicit inline color overrides (white-on-blue primary, translucent-white-on-blue secondary) rather than the existing `accent` variant, which would have rendered invisibly (blue-on-blue) against the new background. |
| C-061 | 2026-09-14 | **Visual Language Reset, Phase B — inner pages (post-Phase-A, same day):** after Phase A (homepage/header/footer) was approved, the lead architect explicitly authorized continuing into every inner page plus mobile, then gave three more concrete defect reports mid-rollout: the header dropdown menus "look like 1990s design," the breadcrumb trail was "too plain," and the Study Abroad/Services hero panel's left/right halves didn't visually match (mismatched backgrounds) while the hero itself was wider than the section below it. All four were fixed and approved (see C-062–C-064 below) before the lead architect authorized rolling the resulting pattern out to the rest of the site, which this entry covers. |
| C-062 | 2026-09-14 | **Nav dropdown menus modernized (`src/components/ui/dropdown-menu.tsx`, `HeaderDesktopNav.tsx`).** The Radix dropdown's default styling (`rounded-lg`, `p-1`, `ring-1`, dense `px-1.5 py-1` items, no visual hierarchy) read as a plain OS context menu. Reworked to `rounded-2xl`, `p-2`, a soft shadow instead of a hairline ring, and larger `px-3 py-2.5` items; the first child in each dropdown (e.g. "All Services", "Overview") is now a highlighted blue "featured" row with a hover-reveal arrow and a separator beneath it, giving each dropdown a clear entry point instead of a flat list. This is the only real consumer of the shared `DropdownMenu*` primitive, so the change is scoped safely. |
| C-063 | 2026-09-14 | **Breadcrumbs redesigned for visual weight (`Breadcrumbs.tsx`).** Previously: plain small gray text, "Home" as a text link, current page as unstyled text — easy to miss entirely. Home is now a house-icon button (`aria-label="Home"`, so `getByRole("link", {name: "Home"})` still passes unchanged); the current page renders as a filled `surface-pale-blue`/`brand-blue-dark` pill instead of plain text, giving the trail actual presence. Used everywhere via the one shared component, so this fixed every page's breadcrumb in one change. |
| C-064 | 2026-09-14 | **New shared `DetailHero` component (`src/components/shared/DetailHero.tsx`) replaces `PageHeader` as the standard inner-page hero, and gains a hub/cluster mode.** `DestinationHero`/`ServiceHero`/`InsuranceHero` (already consolidated onto `DetailHero` earlier in Phase B — see the "Inner-page hero consolidation" note folded into this entry) each render one centered icon for a single-topic page. Hub pages (`/services`, `/study-abroad`, `/insurance`, `/resources`, `/about`, `/scholarships`, `/success-stories`, `/team`, `/events`, `/faq`, and the `/study-abroad/undergraduate`+`/postgraduate` template) were still on bare `PageHeader` (plain text on white, no visual, flagged as "looks bare") — all switched to `DetailHero` too. Since a hub page is about _several_ topics, not one, `DetailHero` gained an `icons?: IconName[]` cluster mode (3 asymmetrically-placed icon badges in blue/mint/warm tones, `CLUSTER_POSITIONS`) alongside its existing single-`icon` mode, rather than forcing every hub page to pick one arbitrary icon. The hero panel now also uses the default `Container` (not `variant="wide"`) so its width matches the section below it, and both hero columns sit inside one shared tinted panel instead of a white-left/tinted-right mismatch (this same fix was applied to the homepage `Hero` first, then carried into `DetailHero` for consistency). Form pages (`/book-consultation`, `/check-eligibility`, `/contact`, `/insurance-quote`) were deliberately left on their existing minimal headers — the brief's own "Form journey" template guidance says no large decorative hero, the form is the content. |
| C-065 | 2026-09-14 | **Dark ink-to-teal gradient card headers replaced sitewide, and the remaining forced-`uppercase` labels removed (Phase B).** Beyond the two already fixed in Phase A (`SectionHeading`, `PageHeader`), a further sweep found six more instances of the same "1990s"-flagged defect pattern — a dark `from-brand-ink to-brand-teal` gradient block behind a white-on-dark icon, and/or a forced-uppercase label — in `DestinationSelectorSection`, `InsuranceCategoriesSection`, `StudyLevelLinksSection`, `ApplicationJourneySection`, `ProcessStepsSection`, `ResourceArticleTemplate`, `DestinationComparisonTable`, `DestinationStudyOptionsSection`, and `PlanningSection`. All were brought onto the established light language: pale-blue/mint tile backgrounds with a white icon-circle floating on top (matching `DetailHero`'s own icon treatment), and `tracking-wide` without `uppercase` (every affected label was already authored in sentence case, so removing the CSS transform was sufficient — no content changes). `ApplicationJourneySection` and `ProcessStepsSection` (previously a plain bordered-box grid) were also converted to the same connected-journey grammar (numbered circle + dashed route line) already used by the homepage's `HowItWorksSection`, so every step-sequence on the site now shares one visual pattern instead of two. |
| C-066 | 2026-09-14 | **Redundant plain grids removed where a richer section already covered the same links.** `/services`' `ServiceCardGrid` (a flat "all 7 services again" grid directly below `ServiceCategoriesSection`, which already links every service under its category) was deleted outright — `ServiceCategoriesSection` was rebuilt as icon-led `surface-pale-blue` panels using each service's full `title` (not `shortTitle`, which was otherwise unused) so no accessible-name regression occurred. Two further dead components from the Phase A homepage rebuild — `EventSection.tsx` and `FeaturedUniversitiesSection.tsx`, both already unreferenced anywhere in `src/app` since Phase A removed them from the homepage but never deleted the files — were deleted as confirmed dead code (CLAUDE.md: unused code should be deleted, not left orphaned). |
| C-067 | 2026-09-14 | **Bespoke closing CTAs consolidated onto the shared `FinalCtaSection`.** `/insurance` had its own plain, white, unstyled "Ready to request a quote?" closing block instead of using the shared `FinalCtaSection` (the bold blue→blue-dark gradient band already used everywhere else) — switched over, fetching `getSiteContent()` in the page body (previously only called inside `generateMetadata`) to supply the required `contact` prop. |
| C-068 | 2026-09-14 | **`StudyLevelTemplate`'s "planning timeline" now reuses `KeyPointGrid` instead of a separate inline bordered-box `<ol>`** — the two were pixel-for-pixel the same pattern authored twice; `content.planningTimeline` is already typed as `KeyPoint[]`, so no content or type change was needed. |
| C-069 | 2026-09-17 | **Switched to a static export (`output: "export"`) so the site can be uploaded directly to plain static hosting (Hostinger shared hosting was the immediate driver).** Every route was already static/SSG with zero API routes or request-time server logic, so this changes packaging, not architecture. Three things required fixing to build cleanly: (1) `src/app/icon.tsx`, `opengraph-image.tsx`, `sitemap.ts`, and `robots.ts` all needed `export const dynamic = "force-static"` — export mode refuses to build a route it can't prove is static ahead of time, even when the route genuinely has no request-time inputs. (2) `next.config.ts`'s `headers()` function is not supported in export mode (there's no server to run it per-request) — the same five security headers now live in a new `public/.htaccess` (Apache `Header always set` directives), copied into `out/.htaccess` on every build; a host that doesn't read `.htaccess` needs the equivalent configured at that host's own layer instead (noted per-platform in docs/DEPLOYMENT.md §17). (3) `next start` errors outright under `output: "export"` and tells you to use a static file server instead — `package.json`'s `"start"` script now runs `serve out -p 3000` (added `serve` as a devDependency) so `npm run build && npm run start` (what `playwright.config.ts`'s `webServer` runs) still works for local/CI testing against the real production artifact. `trailingSlash` was deliberately left at its default `false` rather than set to `true`: `true` would have changed every rendered `<Link>` href to gain a trailing slash, which would have broken roughly two dozen exact-string `href` assertions across the Playwright suite for no real benefit — instead, `public/.htaccess` carries a `mod_rewrite` rule that resolves `/about` → the exported `about.html` on Apache-based hosts. Verified via a full rebuild, all 400 unit/component tests, and all 181 Playwright tests (including accessibility and the full responsive matrix) run against the actual static `out/` output served by `serve`, plus a manual check that `next build` no longer requires a Node server to run at all. docs/DEPLOYMENT.md rewritten throughout (build/start commands, §6 security headers, §17 hosting comparison, new §18 Hostinger-specific upload steps) to match; the "pure static hosting: not compatible" verdict from Phase 9's platform comparison is now reversed. |
| C-070 | 2026-09-17 | **`DirectorySlash Off` added to `public/.htaccess` after a real test deploy to Hostinger caught a defect the local `serve`-based testing in C-069 couldn't — every clean URL (e.g. `/about`) 404'd/403'd in production despite working locally.** Root cause: Next's static export writes an internal per-route directory next to every page's `.html` file (e.g. `about/`, holding React Server Component prefetch payload — not meant to be browsed) for every route, regardless of the `trailingSlash` setting. Apache's default behaviour is to 301-redirect `/about` → `/about/` purely because a same-named directory exists (`mod_dir`'s `DirectorySlash`), which happens _before_ the `mod_rewrite` rule from C-069 gets a chance to resolve it to `about.html` — and `/about/` itself then 403'd (nothing servable inside that directory). `serve` (used for local Playwright testing) has no equivalent directory-redirect behaviour, so this was invisible until the actual Apache-hosted test. Fixed with `DirectorySlash Off` plus a second rewrite rule handling an explicit trailing-slash request, and the original rule's `!-d` exclusion removed (it existed to avoid clobbering "real" directories, but the internal RSC-payload directory must never win over the real page either way). Verified directly against the live `jananconsultants.com` deployment: 21 spot-checked routes all return 200, both `/about` and `/about/` now serve the real page, 404/security-headers/favicon-content-type all confirmed via `curl`, and real client-side `<Link>` navigation confirmed via a Playwright click-through against production. One remaining, non-blocking cosmetic issue found during this same live test: Next's client-side router prefetching requests RSC payload files under a per-segment naming pattern that doesn't match what static export writes to disk, producing harmless 404s in the browser console — page loads and real navigation are unaffected (confirmed by the click-through test); not fixed as part of this entry. |
| C-071 | 2026-09-18 | **Full rebrand from "Asteron Global Consultancy" to "Janan Consultancy"** (lead-architect instruction, the real registered business name matching the `jananconsultants.com` domain). Mechanical text replacement across ~60 files (content, components, tests, docs, `CLAUDE.md`, `package.json`), with two hand-fixed edge cases a blind find-replace couldn't get right: `SiteLogo.tsx`'s screen-reader-only suffix (was building "Janan" + " Global Consultancy" = wrong; fixed to "Janan" + " Consultancy"), and the npm package name / `package-lock.json` (lowercase-kebab, untouched by a case-sensitive replace). `HowAsteronSupportsSection.tsx` renamed to `HowJananSupportsSection.tsx`. All 400 unit/component tests still passed unmodified except two intentional fixtures (`insurance.test.ts`/`insurance/page.test.tsx`'s "never claims to be an insurer" banned-phrase check, updated from `"asteron is an insurer"` to `"janan is an insurer"` so the safety check still matches the real brand name it's guarding against). |
| C-072 | 2026-09-18 | **New logo mark: a "J" stroke ending in a filled destination dot, replacing the open-book/compass-star mark** (`SiteLogo.tsx`, and the matching `icon.tsx`/`opengraph-image.tsx` code-generated assets) — created for the "Janan Consultancy" rebrand (C-071) rather than just re-labelling the old mark, and deliberately reuses the same route/destination-dot visual language as `UniversitySlider`/`DetailHero`'s icon badges (C-073) so the logo reads as this site's own mark, not a generic stock icon. Still a temporary, code-based SVG (no downloaded/commissioned asset), matching the project's ongoing logo policy — see README.md "Logo replacement procedure" for how to swap it later. |
| C-073 | 2026-09-18 | **New homepage hero visual: a single-slide `UniversitySlider` carousel (real, well-known universities named as examples) replaces `HeroVisual`'s abstract route/journey motif** (lead-architect instruction). Real institution names are a deliberate, explicit exception to this project's usual generic-placeholder-only policy for unverified content — confirmed with the lead architect before implementation (a dedicated clarifying question was asked and answered "real, well-known universities... no partnership claim in the copy"). One real university per each of the site's six covered destinations (Oxford/UK, Toronto/Canada, Melbourne/Australia, UCLA/US, TUM/Germany, Trinity College Dublin/Ireland), each with one factual, publicly-verifiable sentence (age, size, or subject strength — no rankings, acceptance rates, or claims Janan cannot verify) and a link to that destination's page — never framed as "our partner" and no logo/trademark use. A visible disclaimer sits directly beneath the slider ("shown for illustration only, not a partnership or admissions guarantee"), and the content-safety test in `home.test.ts` (banned-phrase scan across all homepage content) now covers this array too. Built without a carousel library: one active slide renders at a time (simpler to keep accessible than a multi-slide scroll-snap track), with touch-swipe support since this is the hero's primary mobile visual, an explicit pause/play control (WCAG 2.2.2 "Pause, Stop, Hide" — required for anything that auto-rotates indefinitely), and `prefers-reduced-motion` respected as the default (an explicit user click can still start autoplay — treated as informed, in-the-moment consent rather than the app imposing motion unasked). Reading `prefers-reduced-motion` uses `useSyncExternalStore`, not a `useEffect` + `setState` (which the `react-hooks/set-state-in-effect` lint rule flags as an anti-pattern) — SSR-safe, defaults to "no preference" on the server. `window.matchMedia` had no jsdom polyfill in this project yet (nothing had needed it before); added one to `vitest.setup.ts` following the same pattern as the existing `ResizeObserver`/pointer-capture stubs. The dot slide-indicators were initially just the 6px visible dot as the whole `<button>` — caught by the accessibility Playwright suite (axe `target-size`/WCAG 2.2 SC 2.5.8: interactive targets need a 24×24 CSS-pixel minimum), fixed by keeping the small dot purely visual (`aria-hidden`, nested `<span>`) inside a `size-6` button. The now-fully-superseded `featuredUniversityPlaceholders` content/type/accessor (generic "Institution Placeholder A–D" labels, unused by any page since the Phase A homepage rebuild deleted its only consumer component) and the now-dead `HeroVisual.tsx` were both deleted rather than left orphaned. |
| C-074 | 2026-09-18 | **Real social profile links and email added** (lead-architect-supplied, 2026-09-18): Facebook, LinkedIn, and TikTok profiles plus a WhatsApp Channel broadcast link (distinct from the existing phone-number-based WhatsApp _chat_ link used sitewide, which is unchanged) now render as live links in the footer; `SocialPlatform` gained `"tiktok"` and `"whatsapp"` (lucide-react ships no brand/logo icons for any social platform, confirmed by inspection — kept the existing two-letter-initial-in-a-circle treatment for consistency rather than introducing downloaded trademarked logo assets, extending `platformInitials` with `Tt`/`Wa`). Instagram and YouTube have no confirmed profile yet and stay hidden (`disabled: true`) per the existing placeholder policy. `site.contact.email` changed from the `hello@example.com` placeholder to the real `info@jananconsultancy.com` (note: this differs from the `jananconsultants.com` hosting domain — used exactly as supplied, not "corrected" to match, since domain-vs-mailbox-provider mismatches are a legitimate real-world setup). The Organization JSON-LD (`buildOrganizationJsonLd`, root layout) gained an optional `sameAs` field populated from the same confirmed (non-disabled) social URLs — a genuine SEO enhancement (Google's recommended way to associate an entity with its social profiles) that was correctly withheld while every social link was still a `#` placeholder. The longer real email string exposed a real, pre-existing layout bug: two footer/mobile-menu email links used `inline-flex`/`flex` with no wrap allowance around an un-spaced string, which doesn't wrap in a flex context — caused a uniform ~5px horizontal-overflow failure on every page at exactly the 1024px breakpoint (caught by `e2e/responsive.spec.ts`, not visible at other breakpoints where the footer's 5-column grid had more room). Fixed with `break-all` on both. All 402 unit/component tests and all 181 Playwright tests (accessibility + full responsive matrix) pass after C-071–C-074. |

All 400 unit/component tests and all 181 Playwright tests (accessibility + full 6-breakpoint responsive matrix) pass after C-061–C-068; `npx tsc --noEmit`, `npx eslint .`, and `npx prettier --check .` all clean. Not yet done as of this entry: a dedicated mobile-specific pass over these same inner pages (explicitly the next step the lead architect asked for after inner pages), and a handful of smaller card components (`ResourceCard`, `ScholarshipCard`, `SuccessStoryCard`, `TeamGrid`, `EventCard`, `RelatedServicesSection`) were reviewed and judged already consistent with the new language (no dark gradients, no forced uppercase) rather than rewritten for uniformity's own sake.

## Proposed Decisions (awaiting lead architect sign-off)

| ID    | Date       | Proposal                                                                                                                                                                                                                                                                                                                                               | Rationale                                                                                                                                                   |
| ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P-001 | 2026-09-12 | `Programme` carries an optional denormalized `destinationId` in addition to its `universityId` chain, to simplify destination-based programme filtering.                                                                                                                                                                                               | Avoids expensive joins for a common filter path; see [docs/CONTENT_MODEL.md](CONTENT_MODEL.md) Section 3.                                                   |
| P-002 | 2026-09-12 | Event "registration" in V1 is captured via `GeneralEnquiry` with an optional `relatedEventId` reference, rather than a dedicated `EventRegistration` entity.                                                                                                                                                                                           | Keeps V1 entity count aligned with the specified content model list; avoids scope creep into ticketing/attendance-tracking.                                 |
| P-003 | 2026-09-12 | Success Stories render as rich cards on a single listing page rather than individual detail pages, unless content depth requires otherwise.                                                                                                                                                                                                            | Matches "Success Stories" appearing once in the sitemap scope list without a separate detail-page requirement; simpler to build and maintain for V1 volume. |
| P-004 | 2026-09-12 | Spam/bot protection uses a layered approach: honeypot field + submission-timing heuristic + an optional pluggable CAPTCHA provider behind an interface, rather than committing to one vendor now.                                                                                                                                                      | Avoids vendor lock-in before a provider is approved; see [docs/TECHNICAL_ARCHITECTURE.md](TECHNICAL_ARCHITECTURE.md) Section 5.                             |
| P-005 | 2026-09-12 | Lead status model is a single shared enum/state machine (`NEW → IN_PROGRESS → CONTACTED → QUALIFIED → CONVERTED`, with `CLOSED_LOST` reachable from any non-terminal state) applied uniformly across all four lead entities. **Deferred to Track 3** — there is no persistent lead storage or staff surface in the current track for this to apply to. | Simplifies admin UI and reporting; consistent staff workflow across lead types, once Track 3 introduces both.                                               |

## Unresolved Questions (require lead architect decision)

| ID    | Date raised | Question                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Impact if unresolved                                                                                                                                                                                                 |
| ----- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U-001 | 2026-09-12  | What is Janan's actual legal/registration status, and what jurisdiction(s)' data protection law applies to form submissions?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Blocks accurate legal page content (Privacy Policy, Terms, Cookie Policy) and correct consent-copy wording.                                                                                                          |
| U-002 | 2026-09-12  | Does Janan issue its own insurance quotes, or broker/refer to third-party insurers?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Directly affects `InsuranceProduct.providerDisclosure` content, needed as soon as the Insurance page is built (Track 1, Phase 3).                                                                                    |
| U-003 | 2026-09-12  | What form-submission/email service (Track 2) and hosting platform (Track 1 launch) are approved for use? _(Narrowed after C-011 — media storage and database-host questions no longer apply to the current track.)_                                                                                                                                                                                                                                                                                                                                                                                                                                      | Blocks Track 2 (form delivery) and Track 1 Phase 6 (launch).                                                                                                                                                         |
| U-004 | 2026-09-12  | Is a real logo/brand guideline forthcoming, or should the temporary identity (colours, tagline, logo concept) be treated as final for launch?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Affects whether Design System tokens are final or placeholder-only.                                                                                                                                                  |
| U-005 | 2026-09-12  | What staff roles/permission levels are needed once Strapi (Track 3) introduces an admin surface?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Configured in Strapi's own role system when Track 3 begins; not a current-track blocker.                                                                                                                             |
| U-006 | 2026-09-12  | What is the required data retention period for lead submissions once they are actually stored (Track 2/3), especially for closed/lost records?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Needed before Track 2/3 persists any personal data; not a current-track blocker since nothing is persisted yet.                                                                                                      |
| U-007 | 2026-09-12  | Does Janan have any real, verifiable statistics, testimonials, partnerships, certifications, awards, or accreditations available now, or should all such content remain sample/demo through launch?                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Determines how much content ships labelled as sample vs. real at launch; no fabricated claims will be introduced either way.                                                                                         |
| U-008 | 2026-09-12  | Should "Visa Guidance" get its own page, live under Services, or both, within the current flatter static-site sitemap?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Affects docs/SITEMAP.md and the Services content shape when Track 1 Phase 3 builds that page.                                                                                                                        |
| U-009 | 2026-09-12  | Is a formal consent-record mechanism required for real (non-sample) `SuccessStory` testimonials (e.g., signed release on file, reference ID stored)?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Affects the `SuccessStory` content shape whenever real testimonials are added, and the Track 3 admin workflow once Strapi exists.                                                                                    |
| U-010 | 2026-09-12  | Should lead records maintain a full status-change history log beyond status + assignee, for audit purposes?                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | A Track 3 question — affects `FormInternalNote`/audit design once Strapi introduces persistent lead storage; not a current-track blocker.                                                                            |
| U-011 | 2026-09-13  | Is real, licensed photography (hero + destination/university imagery) available or budgeted for, and if so from where? No photograph is used anywhere on the current homepage or the Study Abroad hub/destination pages — see C-019, C-027, and docs/MEDIA_ATTRIBUTIONS.md.                                                                                                                                                                                                                                                                                                                                                                              | Blocks replacing the CSS/SVG/icon hero and card visuals with real imagery; not a blocker for the current phase's completion.                                                                                         |
| U-012 | 2026-09-13  | Should any of the six Phase 4 destination records be re-verified against live official government/institution sources before this content is treated as launch-ready? This environment authored general-guidance content following the Content Accuracy policy (C-025) without browsing any external sites for Phase 4.                                                                                                                                                                                                                                                                                                                                  | `lastReviewed` dates exist but do not represent live source verification; content should be reviewed against current official sources before being treated as authoritative for real users.                          |
| U-013 | 2026-09-13  | Still unresolved (restated from U-002 now that `/insurance` is built): does Janan issue its own insurance quotes, or broker/refer to third-party insurers, and who are the actual provider(s)? Phase 5 shipped `InsuranceService.providerDisclosure` and the global disclosure with deliberately general, non-committal language pending an answer.                                                                                                                                                                                                                                                                                                      | Once answered, update `providerDisclosure` per insurance type and the "Provider disclosure" section of `docs/MEDIA_ATTRIBUTIONS.md`; not a blocker for Phase 5's completion since no false claim is made either way. |
| U-014 | 2026-09-13  | Are any of the three Phase 5 destination/service cross-links, image assets, or the seven Service records' content ready for real photography, or should the icon/gradient treatment (C-027, extended to Services/Insurance in Phase 5) remain until real assets are approved?                                                                                                                                                                                                                                                                                                                                                                            | Same status as U-011 — no current-phase blocker, but flags where photography would go once available.                                                                                                                |
| U-015 | 2026-09-13  | Phase 7 documented (but explicitly did not implement) the server-side protections a real submission endpoint will need once Track 2 begins: server-side validation mirroring each Zod schema, rate limiting, bot protection, CSRF protection, spam filtering, request-size limits, TLS-only transport, data minimization, a retention policy, access controls, an audit trail, and log redaction of personal fields — see "Form Security Preparation" in docs/TECHNICAL_ARCHITECTURE.md §5. Which of these the chosen Track 2 provider handles natively vs. requires custom implementation is unknown until U-003 (form-submission service) is answered. | Blocks writing a concrete Track 2 security implementation plan; not a current-track blocker since no form makes any request yet.                                                                                     |

---

## Change Log

- 2026-09-12 — Initial creation of decision log during Phase 0 (repository inspection and architecture documentation).
- 2026-09-12 — Phase 1 (application scaffolding): added C-007–C-010 recording stack versions and material deviations from the proposed architecture (Radix over Base UI for shadcn/ui, deferred CSP, deferred structured-logging scaffold, no `loading.tsx` yet).
- 2026-09-12 — **Major scope correction mid-Phase-1** (C-011–C-013): initial release redefined as a static website with no database/auth/admin/CMS; roadmap restructured into four tracks. The `/admin` route was removed. Every project document (README, PRODUCT_REQUIREMENTS, SITEMAP, TECHNICAL_ARCHITECTURE, CONTENT_MODEL, IMPLEMENTATION_PLAN, CLAUDE.md) was updated to match. C-001, C-002, and P-005 marked superseded/deferred rather than deleted; U-003, U-005, U-006, U-008, U-010 narrowed or reframed to the track they now belong to.
- 2026-09-13 — Phase 2 (design system, static content architecture, shared website shell): added C-014–C-017 recording the `ComingSoon` stub-route pattern, the `xl` header breakpoint fix, the social-icon-initials fallback, and the added `/resources/faqs` route. Content architecture expanded (contact info, navigation, announcement, social links, destination/service summaries); README, DESIGN_SYSTEM, TECHNICAL_ARCHITECTURE, CONTENT_MODEL, and IMPLEMENTATION_PLAN updated to match.
- 2026-09-13 — Phase 3 (complete static homepage): added C-018–C-023 and U-011. Homepage content architecture added (`src/content/home.ts` + accessor); 13 homepage section components built; no photography used (documented in new `docs/MEDIA_ATTRIBUTIONS.md`); hand-written `Accordion` primitive (shadcn registry unreachable); a stray `cn` package from a failed registry call was found and removed; an invalid dangling `aria-labelledby` reference was caught and fixed (`SectionHeading` gained `headingId`). README, DESIGN_SYSTEM, CONTENT_MODEL, and IMPLEMENTATION_PLAN updated to match.
- 2026-09-13 — Phase 4 (Study Abroad hub and destination pages): added C-024–C-029 and U-012. `DestinationSummary` removed and unified into a full `Destination` type; `/study-abroad` hub, one shared `/study-abroad/[destination]` template (6 destinations), one shared `StudyLevelTemplate` for undergraduate/postgraduate, and an informative-only `/check-eligibility` page all built on real local content (`src/content/{destinations,study-abroad,eligibility}.ts`). Content Accuracy policy formalized (C-025) and enforced by new content tests; no photography used, extending C-019 (C-027); check-eligibility form deferred with documented reasoning (C-026). README, CONTENT_MODEL, IMPLEMENTATION_PLAN, SITEMAP, and MEDIA_ATTRIBUTIONS updated to match.
- 2026-09-13 — Phase 5 (Services, Visa Guidance, Scholarships and Insurance): added C-030–C-035 and U-013–U-014. `ServiceSummary` removed and unified into a full `Service` type (7 records, one renamed slug, one new scholarship-guidance record); `InsuranceService` implemented for the first time (3 records) with a shared global disclosure; `/services` hub, one shared `/services/[slug]` template (7 services), `/insurance` hub, one shared `/insurance/[slug]` template (3 insurance types), and the first real form in this codebase (`/insurance-quote`, React Hook Form + Zod, a generic demo submission adapter, no network/storage/logging) all shipped on real local content. Required verbatim notices (academic-integrity, visa, scholarship, accommodation) implemented as a prominent `importantNotice` callout. jsdom polyfills added for Radix form primitives (C-033); a Zod-coercion/react-hook-form typing pattern documented (C-034); a `SampleContentBadge` long-label overflow fixed (C-035). README, CONTENT_MODEL, PRODUCT_REQUIREMENTS, TECHNICAL_ARCHITECTURE, IMPLEMENTATION_PLAN, SITEMAP, and MEDIA_ATTRIBUTIONS updated to match.
- 2026-09-13 — Phase 6 (remaining public content pages): added C-036–C-042. `/about`, `/team`, `/success-stories`, `/resources` + 6 articles across 5 categories, `/events`, `/faq` (8 categories), `/contact` (standardized with a new `ContactForm`), `/scholarships`, and the 4 `/legal/*` draft pages all shipped on new local content (`TeamMember`, `SuccessStory`, `ResourceArticle`/`ResourceCategory`, `Event`, `Scholarship`, `FaqCategoryGroup`, `ContactMethod`/`OfficeLocation`, `LegalPage`). `/resources/faqs` (never-built stub) removed in favour of the new `/faq` (C-037); legal content built at the existing `/legal/*` paths rather than a new flat URL set (C-036). No real team members, testimonials, events, or scholarships invented anywhere — every such record is explicitly placeholder/demo/template-labelled (C-038–C-041). A second `SampleContentBadge` overflow fixed (C-042). README, CONTENT_MODEL, SITEMAP, PRODUCT_REQUIREMENTS, TECHNICAL_ARCHITECTURE, IMPLEMENTATION_PLAN, and MEDIA_ATTRIBUTIONS updated to match.
- 2026-09-13 — Phase 7 (static forms, validation and enquiry experience): added C-043–C-046 and U-015. Consolidated all four forms (Consultation, Eligibility, Insurance Quote, Contact) onto one shared architecture — `SubmissionAdapter`/`notConfiguredAdapter` (superseding the Phase 5 `submitDemoForm()`), centralized verbatim copy (`src/lib/forms/messages.ts`), and shared components/hook (`FormField`, `FormErrorSummary`, `FormDemoNotice`, `FormSubmittedNotice`, `ConsentField`, `useDemoFormSubmit`) (C-043). `/book-consultation` (previously `ComingSoon`) and `/check-eligibility` (previously informative-only) both gained real, validated demo forms — `ConsultationForm` and `EligibilityForm` — the latter with conditional English-test/study-gap fields and a native-checkbox destination multi-select, built as a single grouped page rather than a multi-step wizard (C-045, C-046). `InsuranceQuoteForm` gained a coverage-start-date-not-in-past check and a `"not-sure"` insurance type; `ContactForm`'s enquiry types expanded to 8 options. Duplicate page-level demo notices on `/insurance-quote` and `/contact` were removed in favour of the shared `FormDemoNotice` (C-044). Required "Form Security Preparation" documented, not implemented, in docs/TECHNICAL_ARCHITECTURE.md §5 (U-015). No form transmits, logs, or stores personal data; every valid submission still resolves to the required not-connected message. README, PRODUCT_REQUIREMENTS, USER_JOURNEYS, TECHNICAL_ARCHITECTURE, CONTENT_MODEL, and IMPLEMENTATION_PLAN updated to match.
- 2026-09-13 — Phase 8 (production-readiness audit): added C-047. Technical SEO shipped: `sitemap.ts`/`robots.ts` generated from local content excluding drafts/templates/sample-detail pages; safe JSON-LD serialization (`serializeJsonLd()`, escaping every `<`) wired into the shared `JsonLd` component; site-wide `Organization`/`WebSite` JSON-LD added once to the root layout (no per-page duplication); a code-generated default OG image and favicon (`opengraph-image.tsx`, `icon.tsx`) reusing the `SiteLogo` mark, no stock photography. New `src/content/integrity.test.ts` (`npm run validate:content`, 15 cross-cutting content checks — global slug/id uniqueness, cross-reference resolution, valid dates, image/alt-text pairing, no leftover placeholder markers, demo/template/draft label consistency) passed clean on first run. New automated accessibility sweep (`e2e/accessibility.spec.ts`, axe, 17 checks, 0 violations) and a 6-viewport × 13-page responsive matrix (`e2e/responsive.spec.ts`, 78 checks) — the matrix caught a real, previously-undetected 29px header overflow at exactly the `xl` (1280px) breakpoint chosen in C-015, fixed by moving the desktop-nav/mobile-menu split to `2xl` (C-047). New `docs/ROUTE_INVENTORY.md`, `docs/PRODUCTION_CONTENT_CHECKLIST.md` (every placeholder classified Blocker/Important/Enhancement/Future integration), and `docs/DEPLOYMENT.md` (platform-neutral: build/start, env vars, security headers, the CSP decision re-confirmed, forms-security prerequisites for Track 2, cookies/analytics re-verified accurate, dependency-audit summary, post-deploy checklist). `npm audit`: 0 vulnerabilities; `npm outdated`: 8 packages behind latest, none security-relevant, major-version bumps deferred pending compatibility testing rather than performed as a side effect of this audit. README, TECHNICAL_ARCHITECTURE, SITEMAP, and IMPLEMENTATION_PLAN updated to match.
- 2026-09-14 — Phase 10 (mandatory visual redesign): added C-048–C-052. The lead architect rejected the site's visual design (narrow header/footer content width, thin footer link groups, an unremarkable hero, excessive text/card density, a dated feel for a "premium 2026 consultancy"). `Container` gained a `variant` system (`content`/`wide`/`reading`/`fullBleed`, C-048) so the header, footer, announcement bar, and large marketing bands now use a confident ~1600px width instead of the same ~1152px as every other section. The header was rebuilt (bigger logo, wider nav spacing, a secondary phone link, a subtle scroll-aware shadow via a new client `StickyHeaderShell`) and the footer was rebuilt as a substantial full-width closing experience (brand column, a faint route-network motif, icon-led contact details, and — per an explicit link-by-link requirement — every destination, every service, and both non-visitor insurance types now reachable, C-052). The homepage hero was fully recomposed into an asymmetric editorial layout (new headline "Study abroad with a plan built around you.", a layered original SVG visual — glow, route/pins, abstract skyline, floating consultation card — kept inside the `wide` container rather than true viewport-bled, C-049), with no real photography sourced (network was available but Unsplash's keyword-source endpoint is dead; original design work used instead per the brief's own fallback, C-050). `ServicesSection` became one large featured tile plus five compact tiles instead of six identical cards; `HowItWorksSection` became a connected numbered journey with a route line instead of six bordered boxes; `InsuranceSection` replaced its bordered-card grid with an icon-led checklist and a large decorative shield motif; the near-duplicate `WhyChooseSection` was removed from the homepage and deleted as dead code, its content kept for potential future reuse (C-051). Fixed one incidental defect found along the way: a service-card accessible-name regression from an initial whole-card-`<Link>` pattern (reverted to title-only link + `::after` full-card click area, matching the original, more accessible pattern) and 3 Playwright tests that became ambiguous once the footer started duplicating link labels already present in page content (scoped to the `<main>` landmark). All 385 unit/component tests, 174 Playwright tests (including the full accessibility and responsive suites), and a zero-overflow check across 6 breakpoints × 7 pages pass. README, DESIGN_SYSTEM, IMPLEMENTATION_PLAN, and MEDIA_ATTRIBUTIONS updated to match.
- 2026-09-14 — Phase 10B "MANDATORY RESET" (Phase A of a new visual language, same day as and superseding Phase 10): added C-053–C-060. The lead architect rejected Phase 10 outright and issued a complete replacement brief — new palette (C-054), a rebuilt header with a route-ownership active-nav function fixing the "two nav items active simultaneously" defect (C-055), a rebuilt hero and 13-section homepage (C-056), a rebuilt footer capped at 6 links per group with placeholder socials hidden (C-057) — scoped explicitly to Phase A only (header/nav/hero/homepage/footer/mobile), with inner-page redesign deliberately deferred and planned instead in the new `docs/VISUAL_REDESIGN_MATRIX.md`. Per the brief's explicit "inspect screenshots and make further corrections before reporting" instruction, three real defects invisible to the automated suite were caught and fixed during screenshot review: a mobile nav panel rendering at ~75% width instead of near-full-screen, a fixed mobile quick-actions bar partially covering the footer's own last row, and a sitewide forced-uppercase eyebrow style violating the brief's typography rule (C-059) — plus the header/mobile-menu breakpoint moved back from `2xl` to `xl` now that the simplified header no longer needs the extra room C-047 reserved for it (C-058), and `ScholarshipSection`/`FinalCtaSection` brought fully into the new visual language rather than left as Phase 10 leftovers (C-060). All 400 unit/component tests and all 181 Playwright tests (including accessibility and the full responsive matrix) pass; `npm audit` clean; `npx prettier --check .` clean. DESIGN_SYSTEM's color-role table and eyebrow-typography guidance updated to match the new palette and the all-caps removal; README/IMPLEMENTATION_PLAN were not further updated this phase (Phase 10's entries already describe the current architecture patterns; only the visual language changed) — see the Phase A completion report for full before/after detail.
- 2026-09-14 — Phase 10B "Phase B" (inner pages + mobile, same day): added C-061–C-068. After approving Phase A, the lead architect authorized continuing into every inner page, then flagged three more concrete defects mid-rollout — dated-looking nav dropdowns, a too-plain breadcrumb trail, and a hero panel whose two halves didn't visually match and was wider than the section below it — all fixed and re-approved (C-062–C-064) before being carried into the rest of the site. The new shared `DetailHero` component (replacing `PageHeader`) now powers every inner-page hero, with a new icon-cluster mode for hub/router pages that cover several topics at once; six more instances of the flagged "dark gradient block" card pattern and several more forced-uppercase labels were found and fixed sweeping the rest of the site (C-065); a fully redundant flat services grid and two already-dead homepage-section components were deleted (C-066); a bespoke closing CTA was consolidated onto the shared `FinalCtaSection` (C-067). All 400 unit/component tests and all 181 Playwright tests (accessibility + full responsive matrix) pass. Mobile-specific pass not yet started as of this entry — explicitly the next step requested.
- 2026-09-17 — Phase 10B "Phase C" (static export for Hostinger deployment): added C-069. The lead architect chose Hostinger (shared/static hosting) over Vercel for production, which required switching the app from a Next.js server build to a genuine static export (`output: "export"`) — a packaging change, not an architecture change, since every route was already static/SSG. Fixed the build errors this exposed (`force-static` on the four metadata-route files), moved security headers from `next.config.ts`'s `headers()` (unsupported in export mode) to a new `public/.htaccess`, and replaced `next start` (which now errors outright) with `serve out` for local/CI preview. docs/DEPLOYMENT.md rewritten to match, including a new Hostinger-specific upload walkthrough, and its Phase 9 "pure static hosting: not compatible" verdict reversed. Verified via a full rebuild and the complete 400-test/181-test suite run against the real static output.
- 2026-09-17 — Same-day follow-up (C-070): a real test deploy to Hostinger (`jananconsultants.com`) exposed a defect C-069's local testing couldn't catch — every clean URL 404'd/403'd in production. Root cause and fix documented in C-070: Apache's automatic directory-slash redirect was firing on Next's internal per-route RSC-payload directories before the rewrite rule could resolve the real page. Fixed in `public/.htaccess`, re-verified against the live site (21 routes, security headers, favicon, 404 handling, and a real click-through navigation test all confirmed working).
- 2026-09-18 — Rebrand + hero university slider + real contact links (local changes only — explicitly not deployed to production per the lead architect's standing instruction): added C-071–C-074. Full rebrand from "Asteron Global Consultancy" to the real business name "Janan Consultancy" across ~60 files, plus a new "J" + destination-dot logo mark replacing the old book/compass (C-071, C-072). The homepage hero's abstract route-motif visual was replaced with a single-slide, auto-advancing `UniversitySlider` carousel naming one real, well-known university per covered destination as an illustrative example — a deliberate, lead-architect-confirmed exception to the project's usual generic-placeholder-only policy, with a visible no-partnership disclaimer and full WCAG 2.2 compliance (pause control, reduced-motion respect, 24×24px minimum touch targets on the dot indicators after an accessibility-suite catch) (C-073). Real Facebook/LinkedIn/TikTok/WhatsApp-Channel links and a real email address were added; the longer real email exposed and fixed a genuine sitewide 1024px-breakpoint overflow bug in two flex-based contact links that couldn't wrap (C-074). All 402 unit/component tests and all 181 Playwright tests (accessibility + full responsive matrix) pass.
