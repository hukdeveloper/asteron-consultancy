# Copy this file to .env.local for local development.
# Never commit a file containing real secrets.

# Public base URL of the site. Used for absolute URLs in metadata
# (Open Graph, canonical links, sitemap). Falls back to a local
# development URL when unset — see src/lib/env.ts.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
